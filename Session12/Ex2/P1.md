Vấn đề của statement Nối chuỗi

```Java
String sql = "UPDATE patients SET temperature = " + temp + ", heart_rate = " + hearRate + " WHERE id = " + id;
```
Nếu nhập: 37,5   (theo locale VN/Pháp)
SQL thành: UPDATE patients SET temperature = 37,5

=> SQL hiểu:
•	37 và 5 là 2 giá trị khác nhau
 LỖI cú pháp ngay


🛡️ Vì sao PreparedStatement giải quyết được?
- PreparedStatement tách biện hoàn toàn SQL và dữ liệu

Cơ chế hoạt động

1. Pre - compile
UPDATE patients set temperature = ?, hear_rate = ?
Database chỉ hiểu:
•	cấu trúc query
•	KHÔNG quan tâm giá trị

Binding dữ liệu
```java
ps.setDouble(1, temperature);
ps.setInt(2, heartRate);
ps.setInt(3, patientId);
```
	•	tự convert double → format đúng SQL
	•	LUÔN dùng dấu . chuẩn SQL
	•	KHÔNG phụ thuộc locale máy

setDouble() và setInt():
•	chuyển dữ liệu sang dạng chuẩn DB
•	bỏ qua vấn đề dấu , hay .
•	tránh lỗi cú pháp + tăng độ an toàn
