Code kiểu cũ 
```sql
SELECT * FROM doctors 
WHERE doctor_code = 'input' 
AND password = 'input'
```


Nếu User nhập: ' OR ' 1'='1

Query thành:

```sql
SELECT * FROM doctors 
WHERE doctor_code = '' 
AND password = '' OR '1'='1'
```

Điều kiện '1'='1' luôn đúng
=> Logic Bypass -> vào hệ thống


PreparedStatement hoạt động thế 2 bước tách biệt hoàn toàn
 Bước 1: Pre-Compiled 
```sql
SELECT * FROM doctors WHERE doctor_code = ? AND password = ?
```
Database 
- Parse SQL
- Xác định structure
- Không biết giá trị input

Bước 2: Gán Giá trị
```java
ps.setString(1, doctorCode);
ps.setString(2, password);
```
- Input được truyền vào như data thuần
- KHÔNG được coi là SQL

=> Cách cũ dễ inject, 
=> cách cũ inject SQL , Cách mới input = dữ liệu

